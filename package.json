{
  "name": "videos",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "develop:backend": "yarn --cwd backend develop",
    "develop:frontend": "wait-on http://localhost:1337/admin && yarn --cwd frontend start",
    "develop": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.3",
    "@commitlint/config-conventional": "^16.2.1",
    "cross-env": "7.0.3",
    "husky": "^7.0.0",
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  },
  "dependencies": {
    "react-router-dom": "^6.3.0"
  }
}
